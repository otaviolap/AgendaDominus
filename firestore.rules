rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acesso completo para desenvolvimento
    // IMPORTANTE: Para produção, use regras mais restritivas
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
